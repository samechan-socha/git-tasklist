<h1>タスク一覧</h1>
<h3>(<%= @user.name %>でログイン中)</h3>

<% if @tasks %>
  <%= render 'tasks/tasks', tasks: @tasks %>
<% else %>
  <p>タスクはありません</p>
<% end %>

<%= link_to '新規タスク', new_task_path, class: "btn btn-primary" %>